# Runtime dependencies for the memory service.
fastapi==0.115.6
uvicorn[standard]==0.34.0
asyncpg==0.30.0
pgvector==0.3.6
fastembed==0.5.1
numpy==2.2.1
# Used only by the optional LLM extraction path (Gemini/Anthropic/OpenAI over
# REST). Imported lazily; the offline service never needs it.
httpx==0.28.1
